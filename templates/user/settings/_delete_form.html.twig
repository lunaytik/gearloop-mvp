<form method="post" action="{{ path('app_settings_delete_account') }}" onsubmit="return confirm('Are you sure you want to delete your account, deletion will be definitive ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-error">Delete account</button>
</form>
