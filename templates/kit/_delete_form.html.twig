<form method="post" action="{{ path('app_kit_delete', {'id': kit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kit.id) }}">
    <button class="btn btn-error mt-4">Delete</button>
</form>
